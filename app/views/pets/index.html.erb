<h1>All Pets for <%= @owner.first_name %></h1>
<hr><% @pets.each do |pet| %> </hr>
<hr><%= link_to pet.name, pet_path(pet.id) %></hr>
<hr><% end %></hr>

<hr><%= link_to "Add a new pet", new_owner_pet_path %></hr>
